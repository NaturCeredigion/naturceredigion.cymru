[build]
publish = "public"
command = "git submodule update --init --remote && npm install && npx postcss --version && npm run build"

[build.environment]
HUGO_VERSION = "0.149.0"
GO_VERSION = "1.20.5"
HUGO_BASEURL = "/"
